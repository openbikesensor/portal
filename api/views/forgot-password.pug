extends layout.pug

block title
  | Reset Password

block content
  form(method="post")
    fieldset
      label(for="email") E-Mail Address
      input(id="email", name="email")

    button(type="submit") Send recovery mail
